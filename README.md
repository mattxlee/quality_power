# PoC algorithm: Quality and power calculation.

Quality and power calculation sample for PoC kernel algorithm.

* Build with g++ or clang+
* Boost library required
* Link with `gmp` and `mpfr` libraries
* CMake 2.7 or higher
